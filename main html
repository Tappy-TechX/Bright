<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Happy Girlfriend's Day Babe ♥️💍</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      /* Set the background image */
      background-image: url('https://files.catbox.moe/ajzx2n.jpg');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      
      color: white;
      overflow: hidden;
    }

    /* New overlay style to darken the background image slightly */
    .background-overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.5); /* 50% black overlay */
      z-index: -1; /* Places it above the body background but below all content */
    }

    .container {
      z-index: 1;
      position: relative;
      height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 2rem;
    }
    
    .neon-box {
      background: rgba(20, 20, 20, 0.4);
      padding: 2.5rem;
      border-radius: 20px;
      border: 1px solid #ff66cc;
      max-width: 900px;
      width: 90%;
      box-shadow: 0 0 5px #ff66cc, 0 0 15px #ff66cc, 0 0 25px #ff3399, 0 0 35px #ff3399;
      animation: neon-glow 2s infinite alternate, fadeIn 2s ease-in-out;
    }

    h1 {
      font-size: 3rem;
      color: #ff66cc;
      margin-bottom: 1rem;
      animation: glow 2s infinite alternate;
    }

    p {
      font-size: 1.5rem;
      max-width: 800px;
      color: #ffffffdd;
      line-height: 1.8;
      animation: none; 
    }
    
    .quote-container {
      margin-top: 1.5rem;
      padding: 1rem;
      height: 120px; 
      display: flex;
      align-items: center;
      justify-content: center;
      transition: opacity 1s ease-in-out;
    }

    .quote-container p {
      font-size: 1.2rem;
      font-style: italic;
      color: #ffffffaa;
      max-width: 700px;
      animation: none;
    }
    
    @keyframes glow {
      from {
        text-shadow: 0 0 10px #ff66cc, 0 0 20px #ff99cc;
      }
      to {
        text-shadow: 0 0 20px #ff66cc, 0 0 30px #ff3399;
      }
    }
    
    @keyframes neon-glow {
      from {
        box-shadow: 0 0 5px #ff66cc, 0 0 15px #ff66cc, 0 0 25px #ff3399, 0 0 35px #ff3399;
        border-color: #ff66cc;
      }
      to {
        box-shadow: 0 0 10px #ff66cc, 0 0 25px #ff66cc, 0 0 40px #ff3399, 0 0 50px #ff3399;
        border-color: #ff99cc;
      }
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: scale(0.95);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    .emoji-bg {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      overflow: hidden;
      z-index: 0; /* Emojis float above the background overlay */
    }

    .emoji {
      position: absolute;
      font-size: 2rem;
      animation: float 12s linear infinite;
      opacity: 0.7;
    }

    @keyframes float {
      0% {
        transform: translateY(100vh) rotate(0deg);
        opacity: 0;
      }
      50% {
        opacity: 1;
      }
      100% {
        transform: translateY(-100vh) rotate(360deg);
        opacity: 0;
      }
    }

    @media (max-width: 600px) {
      .neon-box {
        padding: 1.5rem;
        width: 95%;
      }
      h1 {
        font-size: 2.2rem;
      }
      p {
        font-size: 1.1rem;
      }
      .quote-container {
        height: 140px;
      }
      .quote-container p {
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>

<!-- This new div creates the dark overlay -->
<div class="background-overlay"></div>

<div class="emoji-bg" id="emoji-bg"></div>

<div class="container">
  <div class="neon-box">
    <h1>Happy Girlfriend's Day ♥️ Babe</h1>
    <p>
      From the moment you walked into my life, everything changed. <br>
      Your smile lights up my darkest days, and your love fills my heart with joy. <br>
      You are my dream, my sunshine, my peace, and my forever. <br>
      I love you more with every heartbeat. 💖🌹<br>
      Thank you for being the most amazing girlfriend ever. 💍💋
    </p>
    
    <div id="quote-container" class="quote-container">
      <!-- Quotes will be injected here by JavaScript -->
    </div>
  </div>
</div>

<script>
  // --- Emoji Background Script ---
  const emojiList = ['💖','💘','💕','🌹','💍','😍','❤️','🩷','💋','😘'];
  const colors = ['#ff69b4', '#ff1493', '#db7093', '#ff4500', '#ff6347', '#dc143c'];

  function createEmoji() {
    const emoji = document.createElement('div');
    emoji.classList.add('emoji');
    emoji.innerText = emojiList[Math.floor(Math.random() * emojiList.length)];
    emoji.style.left = Math.random() * 100 + 'vw';
    emoji.style.top = Math.random() * 100 + 'vh';
    emoji.style.fontSize = Math.random() * 20 + 20 + 'px';
    emoji.style.color = colors[Math.floor(Math.random() * colors.length)];
    emoji.style.animationDuration = 10 + Math.random() * 10 + 's';
    document.getElementById('emoji-bg').appendChild(emoji);

    setTimeout(() => {
      emoji.remove();
    }, 20000);
  }

  setInterval(createEmoji, 300);

  // --- Love Quotes Script ---
  const loveQuotes = [
    "\"In your light, I learn how to love. In your beauty, how to make poems. You dance inside my chest where no one sees you, but sometimes I do, and that sight becomes this art.\" - Rumi",
    "\"I love you not only for what you are, but for what I am when I am with you. I love you for the part of me that you bring out.\" - Elizabeth Barrett Browning",
    "\"I saw that you were perfect, and so I loved you. Then I saw that you were not perfect and I loved you even more.\" - Angelita Lim",
    "\"To the world, you may be one person, but to one person you are the world.\" - Dr. Seuss",
    "\"If I know what love is, it is because of you.\" - Hermann Hesse",
    "\"You are my today and all of my tomorrows.\" - Leo Christopher"
  ];

  const quoteContainer = document.getElementById('quote-container');
  let currentQuoteIndex = 0;

  function changeQuote() {
    quoteContainer.style.opacity = 0;
    setTimeout(() => {
      currentQuoteIndex = (currentQuoteIndex + 1) % loveQuotes.length;
      quoteContainer.innerHTML = `<p>${loveQuotes[currentQuoteIndex]}</p>`;
      quoteContainer.style.opacity = 1;
    }, 1000);
  }

  // Set initial quote and start the rotation
  quoteContainer.innerHTML = `<p>${loveQuotes[currentQuoteIndex]}</p>`;
  quoteContainer.style.opacity = 1;
  setInterval(changeQuote, 6000);
</script>

</body>
</html>
